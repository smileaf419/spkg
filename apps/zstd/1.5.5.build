####
## zstd build
## Copyright 2023 spkg developers, licensed under GPLv2
HOMEPAGE="https://facebook.github.io/zstd/"
DESCRIPTION="Zstandard is a fast compression algorithm, providing high compression ratios."
LICENSE="GPLv2"
SRC_URI=("https://github.com/facebook/zstd/releases/download/v${PKG_VER}/zstd-${PKG_VER}.tar.gz")
[[ $VERIFY_SIG == 1 ]] && SRC_URI+=("https://github.com/facebook/zstd/releases/download/v${PKG_VER}/zstd-${PKG_VER}.tar.gz.sig")
PATCHES=()
DEPS="apps/xz-utils system/zlib"
BDEPS=""
RDEPS=""
ARCHS="x86 x86_64"
SLOT=0
MULTILIB="64 32 x32"

src_configure() { :; }

src_compile() {
	if [[ ! -z $LIB ]]; then
		export CC="gcc -m$LIB"
	fi
	compile_default prefix=/usr libdir=/lib exec_prefix=/
}

src_install() {
	#PKGCONFIGDIR="/usr/lib/pkgconfig"
	if [[ -z $LIB ]]; then
		install_default prefix=/usr libdir=/lib exec_prefix=/
		rm -v "$ROOT/$D"/lib/libzstd.a
	else
		make prefix=/usr libdir=/lib DESTDIR=$PWD/DESTDIR install
		mkdir -p $D/lib$LIB
		cp -Rv DESTDIR/lib/* $D/lib$LIB/
		sed -e "/^libdir/s/lib$/lib$LIB/" -i $D/lib$LIB/pkgconfig/libzstd.pc
		rm -rf DESTDIR
	fi
}
